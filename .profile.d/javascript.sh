# NodeJS 
# 
# installed via: https://nodejs.org/en/download/current
# config file: ${HOME}/.npmrc

# add node install -g location to path
if [ -d "$HOME/.nodejs/bin" ]; then
    PATH="$HOME/.nodejs/bin:$PATH"
fi;
